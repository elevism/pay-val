/**
 * Validate expiration month and year
 */
export declare const validateExpiration: (month: string, year: string) => boolean;
/**
 * Validate the expiration month
 */
export declare const validateExpirationMonth: (month: string) => boolean;
/**
 * Validate the expiration year
 */
export declare const validateExpirationYear: (year: string) => boolean;
